MATCH (n)
DETACH DELETE n

match (n) RETURN n

USING PERIODIC COMMIT 500
LOAD CSV WITH HEADERS FROM "https://github.com/joseleonec/nosql/blob/master/datos.csv" AS csvLine
CREATE (c:Cliente {idCliente: toInteger(csvLine.idCliente), edad: toInteger(csvLine.edad) })
CREATE (s:Solicitud {idSolicitud: toInteger(csvLine.idSolicitud), fecha: csvLine.fecha, fechaCompra: csvLine.fechaCompra })
CREATE (n:Negociacion {idNegociacion: toInteger(csvLine.idNegociacion), estado: csvLine.estado})
CREATE (c)-[:realiza]->(s)
CREATE (s)-[:implica]->(n)

LOAD CSV WITH HEADERS FROM "https://github.com/joseleonec/nosql/blob/master/cliente.csv" AS csvLine
CREATE (c:Cliente {idCliente: toInteger(csvLine.idCliente), edad: toInteger(csvLine.edad) })

LOAD CSV WITH HEADERS FROM "https://github.com/joseleonec/nosql/blob/master/solicitud.csv" AS csvLine
CREATE (s:Solicitud {idSolicitud: toInteger(csvLine.idSolicitud), fecha: csvLine.fecha, fechaCompra: csvLine.fechaCompra })

USING PERIODIC COMMIT 500
LOAD CSV WITH HEADERS FROM "https://github.com/joseleonec/nosql/blob/master/negociacion.csv" AS csvLine
CREATE (n:Negociacion {idNegociacion: toInteger(csvLine.idNegociacion), estado: csvLine.estado})

CREATE 
  (`0` :Factura {idFactura:'1'}) ,
  (`1` :Producto {nombre:'ipad',costo:'100',peso:'1Kg'}) ,
  (`3` :Envio {idEnvio:'1'}) ,
  (`4` :Ubicacion {Calle:'Av Loja'}) ,
  (`5` :Parroquia {idParroquia:'1',nombre:'Yanuncay'}) ,
  (`1`)-[:`aparece` ]->(`0`),
  (`0`)-[:`significa` ]->(`3`),
  (`3`)-[:`llega` ]->(`4`),
  (`4`)-[:`esta_en` ]->(`5`)

CREATE 
  (`0` :Factura {idFactura:'2'}) ,
  (`1` :Producto {nombre:'arroz',costo:'1',peso:'1Kg'}) ,
  (`3` :Envio {idEnvio:'2'}) ,
  (`4` :Ubicacion {Calle:'Av Loja'}) ,
  (`5` :Parroquia {idParroquia:'1',nombre:'Yanuncay'}) ,
  (`1`)-[:`aparece` ]->(`0`),
  (`0`)-[:`significa` ]->(`3`),
  (`3`)-[:`llega` ]->(`4`),
  (`4`)-[:`esta_en` ]->(`5`)
CREATE 
  (`0` :Factura {idFactura:'3'}) ,
  (`1` :Producto {nombre:'ipad',costo:'100',peso:'1Kg'}) ,
  (`3` :Envio {idEnvio:'3'}) ,
  (`4` :Ubicacion {Calle:'Av Loja'}) ,
  (`5` :Parroquia {idParroquia:'1',nombre:'Yanuncay'}) ,
  (`1`)-[:`aparece` ]->(`0`),
  (`0`)-[:`significa` ]->(`3`),
  (`3`)-[:`llega` ]->(`4`),
  (`4`)-[:`esta_en` ]->(`5`)

CREATE 
  (`0` :Factura {idFactura:'4'}) ,
  (`1` :Producto {nombre:'ipad',costo:'100',peso:'1Kg'}) ,
  (`3` :Envio {idEnvio:'4'}) ,
  (`4` :Ubicacion {Calle:'Av Loja'}) ,
  (`5` :Parroquia {idParroquia:'1',nombre:'Yanuncay'}) ,
  (`1`)-[:`aparece` ]->(`0`),
  (`0`)-[:`significa` ]->(`3`),
  (`3`)-[:`llega` ]->(`4`),
  (`4`)-[:`esta_en` ]->(`5`)

CREATE 
  (`0` :Factura {idFactura:'5'}) ,
  (`1` :Producto {nombre:'ipad',costo:'100',peso:'1Kg'}) ,
  (`3` :Envio {idEnvio:'5'}) ,
  (`4` :Ubicacion {Calle:'Av Loja'}) ,
  (`5` :Parroquia {idParroquia:'1',nombre:'Yanuncay'}) ,
  (`1`)-[:`aparece` ]->(`0`),
  (`0`)-[:`significa` ]->(`3`),
  (`3`)-[:`llega` ]->(`4`),
  (`4`)-[:`esta_en` ]->(`5`)

CREATE 
  (`0` :Factura {idFactura:'6'}) ,
  (`1` :Producto {nombre:'ipad',costo:'100',peso:'1Kg'}) ,
  (`3` :Envio {idEnvio:'6'}) ,
  (`4` :Ubicacion {Calle:'Av Loja'}) ,
  (`5` :Parroquia {idParroquia:'1',nombre:'Yanuncay'}) ,
  (`1`)-[:`aparece` ]->(`0`),
  (`0`)-[:`significa` ]->(`3`),
  (`3`)-[:`llega` ]->(`4`),
  (`4`)-[:`esta_en` ]->(`5`)

USING PERIODIC COMMIT 500
LOAD CSV WITH HEADERS FROM "https://github.com/joseleonec/nosql/blob/master/datos.csv" AS csvLine
MATCH (person:Person { id: toInt(csvLine.personId)}),(movie:Movie { id: toInt(csvLine.movieId)})
CREATE (person)-[:PLAYED { role: csvLine.role }]->(movie)